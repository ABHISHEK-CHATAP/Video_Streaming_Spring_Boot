spring.application.name=spring-streaming-backend
server.port=9090

# In ChatGpt write => spring boot mysql jpa config => press enter

spring.datasource.url=jdbc:mysql://localhost:3306/streaming
spring.datasource.username=root
spring.datasource.password=root1234


# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#maximum file size allowed for a single file upload
spring.servlet.multipart.max-file-size=1000MB

#Maximum request size allowed for multipart/form-data requests
spring.servlet.multipart.max-request-size=1000MB

files.video=videos/
